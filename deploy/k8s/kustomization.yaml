apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: k8up-btl
resources:
  - namespace.yaml
  - rbac.yaml
  - pvc.yaml
  - deployment.yaml
  - networkpolicy.yaml
labels:
  - pairs:
      app.kubernetes.io/name: k8up-btl
      app.kubernetes.io/part-of: k8up-btl
    includeSelectors: false
images:
  - name: ghcr.io/d-kholin/k8up-btl
    # Placeholder. Consume this directory as a kustomize remote base pinned to
    # a release ref and override the tag in your overlay (see
    # docs/getting-started.md), or set a real tag here for direct apply.
    newTag: "1.x.x"